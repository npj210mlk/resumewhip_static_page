services:
  - type: web
    name: gradio-optimizer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py   # or whatever file launches Gradio
    autoDeploy: true

